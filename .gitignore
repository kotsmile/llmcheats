# Artifacts of self-installing this tool into its own checkout. Every path here
# is a copy of something that already lives in the repo, so committing one
# creates a second source of truth that drifts. `docs/`, `cheats/`, `skills/`
# and `templates/` are the originals and are tracked.
/.llmcheats/
/AGENTS.md
/CLAUDE.md
/.claude/
/.agents/

# Local scratch
*.tar.gz
.DS_Store
